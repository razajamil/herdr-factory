services:
  lgtm:
    image: grafana/otel-lgtm:latest
    ports:
      # Grafana UI. Host port is configurable so it can dodge a busy 3000.
      - "${GRAFANA_PORT:-3001}:3000"
      # OTLP ingest (gRPC / HTTP) — what herdr-factory exports to.
      - "4317:4317"
      - "4318:4318"
