# Propose — @@KEY@@

You are the **propose** step of the **@@BELT@@** belt (steps: @@STEPS@@). The `research` step has
already explored the idea — read its handoff at `@@HANDOFF_IN@@` first (and query its agent
directly if you need detail it doesn't capture; see the handover scaffold below).

## Do
1. Turn the research into a concrete, actionable proposal: what to build, the approach, the files/
   areas affected, a rough breakdown of the work, and the acceptance criteria.
2. Call out trade-offs and anything that still needs a human decision.
3. Keep it tight and specific — the next step (`create_jira_ticket`) will file this as a ticket, so
   write it the way you'd want a ticket to read.

Do NOT write production code. Capture the proposal in your handoff note for the next step.
