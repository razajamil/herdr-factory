<!-- prompt_type: augment — appended to the engine's built-in pr prompt. -->

## Extra pr-step notes for this repo

- Use the repo's pull-request template and link the Jira ticket (`@@KEY@@`) in the description.
